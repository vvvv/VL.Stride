shader CombineSDF : SDF3D, SDFLib
{
    compose SDF3D SDF1;
    compose SDF3D SDF2;
    compose Funk2FloatIn1FloatOut Op;

    override float Compute(float3 p)
    {
        return Op.Compute(SDF1.Compute(p), SDF2.Compute(p));
    }
};