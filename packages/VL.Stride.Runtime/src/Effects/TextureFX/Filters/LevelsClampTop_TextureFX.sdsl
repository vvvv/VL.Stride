shader LevelsClampTop_TextureFX : Levels_TextureFX
{
    stage override float4 Clamp(float4 col)
    {
        return min(col,1);
    }

    stage override float4 Shading()
    {
        base.Shading();
    }
};


