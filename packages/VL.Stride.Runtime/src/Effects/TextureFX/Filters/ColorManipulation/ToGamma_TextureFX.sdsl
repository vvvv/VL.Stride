shader ToGamma_TextureFX : TextureFX
{
    //https://learnopengl.com/Advanced-Lighting/Gamma-Correction
    float Gamma = 2.2f;
    
    stage override float4 Shading()
    {
        float4 col = InTex();
        col.rgb = pow(col.rgb, 1/Gamma);
        return col;
    }
};