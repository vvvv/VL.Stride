[Category("Filter")]
[Summary("Get the Highlights of a texture")]
[Tags("Color Grading")]

shader GetHighlights_TextureFX : FilterBase, ColorGradingOperations
{
    float4 Filter(float4 tex0col)
    {
        return GetHighlights(tex0col);
    }
};