shader LinearToGamma_TextureFX : TextureFX, ColorUtilityTemp
{
    float Gamma = 2.2f;
    
    stage override float4 Shading()
    {
        float4 col = InTex();
        col.rgb = LinearToGamma(col, Gamma);
        return col;
    }
};