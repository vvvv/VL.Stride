shader Levels_ClampBoth_TextureFX : Levels_TextureFX
{
    stage override float4 Clamp(float4 col)
    {
        return saturate(col*toBlack);
    }

    stage override float4 Shading()
    {
        return base.Shading();
    }
};


