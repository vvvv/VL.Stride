[TextureSource]
[Category("Source")]
[Summary("UV Coordinate as texture")]
shader UV_TextureFX : TextureFX
{
    stage override float4 Shading()
    {
        float2 uv = streams.TexCoord;

        return float4(uv.x, uv.y, 0.0, 1.0);
    }
};