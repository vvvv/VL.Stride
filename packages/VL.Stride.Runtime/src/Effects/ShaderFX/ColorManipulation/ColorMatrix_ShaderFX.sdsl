[Summary("Color Matrix")]
[Tags("Color Grading")]

shader ColorMatrix_ShaderFX : ComputeFloat4, ColorMatrixOperations
{
    [Default(1.0, 1.0, 1.0, 1.0)]
    compose ComputeFloat4 Color;

    override float4 Compute()
    {
        return ColorMatrix(Color.Compute());
    }
};