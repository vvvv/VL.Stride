shader ConstantColorTexturedScreenSpace_VLNode : ConstantColorTextured_VLNode, Texturing
{
    override stage void VSMain()
    {
        streams.ShadingPosition = mul(streams.Position, Projection);
    }
};
